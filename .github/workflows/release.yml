name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  placeholder:
    name: Release placeholder
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Release workflow - to be implemented in Phase 6"
